import React from "react";
import Styled from "styled-components";

const ProfileInHeader = () => {
  return <Div>this is profile in header</Div>;
};

export default ProfileInHeader;

const Div = Styled.div`

background: lightgray;
padding: 1rem;
border-radius: 1rem;
`;
